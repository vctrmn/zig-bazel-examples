load(":zls.bzl", "create_zls_toolchain_targets", "zig_runner", "zls_runner")

toolchain_type(name = "toolchain_type")

create_zls_toolchain_targets(name = "all")

zig_runner(
    name = "zig",
)

zls_runner(
    name = "zls",
    zig = ":zig",
)
